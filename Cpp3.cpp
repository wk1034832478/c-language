#include<stdio.h>

// 函数定义（声明）: 用来返回最大值
int max( int num1, int num2 );

// 调用函数来完成定义的任务，函数定义就像是指挥，调用函数就像是冲锋陷阵的士兵
int max( int num1, int num2 ) {
	if ( int num1, int num2 ) {
		if ( num1 > num2 ) {
			return num1;
		} else {
			return num2;
		}
	}
}